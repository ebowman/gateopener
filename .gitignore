
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Swift / Xcode build artifacts
.build/
.swiftpm/
DerivedData/
*.xcuserdatad
.DS_Store
*.app
*.dmg

# Generated app bundle assets (regenerate with scripts/generate-icon.sh /
# scripts/build-app.sh; only the .svg source is committed).
*.iconset/
Resources/AppIcon.icns

# Secrets
.env
.env.*
!.env.example
*.p8
*.pem
*.p12
*.cer
.comelit_token.json
*_token.json
*token_cache*

# Generated by scripts/publish-release.sh at release time. Ignored so a
# successful release does not dirty the tree and trip the next release's
# clean-tree precondition.
appcast.json
